---
title: "Learning from Your Own Mistakes: Constructing Learnable Micro-Reflective Trajectories for Self-Distillation"
title_zh: 从自身错误中学习：为自蒸馏构建可学习的微反思轨迹
authors: "Zhilin Huang, Hang Gao, Ziqiang Dong, Yuan Chen, Yifeng Luo, Chujun Qin, Jingyi Wang, Yang Yang, Guanjun Jiang"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.18844v1"
tags: ["query:distillation"]
score: 9.0
evidence: 基于轨迹增强的语言模型自蒸馏
tldr: 现有自蒸馏通过隐式logit对齐提升大模型推理能力，但缺乏对模型错误的诊断与纠正。本文提出TAPO，利用正确与错误rollout的对比结构，在失败点插入自然语言诊断和修正，构建可学习的微反射轨迹。在AIME 2024、AIME 2025和HMMT 2025上，TAPO在相同训练步数下一致优于GRPO。该方法将自蒸馏从隐式分布对齐推进到显式轨迹构建，显著增强了模型的首遍推理和错误纠正能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18844v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1404, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18844v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 894, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18844v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18844v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1162, \"height\": 1178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18844v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1446, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18844v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1442, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18844v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18844v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1332, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有自蒸馏无法对模型错误进行细粒度诊断和纠正，仅通过KL散度隐式对齐分布。
method: TAPO利用正确与错误rollout的对比结构，在失败点插入自然语言诊断与修正，构建强化学习的微反射轨迹。
result: 在AIME 2024/2025和HMMT 2025上，TAPO在相同训练步数下一致优于GRPO。
conclusion: TAPO通过显式错误修正轨迹增强了模型的首遍推理和纠错能力，推动了自蒸馏范式进步。
---

## 摘要
自蒸馏通过将模型自身的展开作为训练信号来提升大型语言模型的推理能力，通常采用隐式的logit级对齐，最小化与特权目标分布的KL散度。然而，由于这种监督通过无控制采样生成，它无法提供关于模型特定错误的诊断性洞察，也无法针对其个体失败模式提供纠正性指导。因此，模型学会模仿特权分布，而非获得细粒度的修正，以指出其推理失败的位置和原因。在本文中，我们提出轨迹增强策略优化（TAPO），将自蒸馏从隐式分布对齐推进到显式轨迹构建。在RL训练期间，模型对同一查询生成正确与错误的展开，TAPO利用这种对比结构构建微反思修正：新的训练轨迹保留模型在失败点之前的错误推理，然后插入由同一采样组中正确参考引导的自然语言诊断和修正推理。由于每条轨迹都锚定在学习者自身的前缀和解决方案上，与基于KL的方法施加的位置级对齐相比，修正信号更大程度地保留了模型的在策略分布。为整合这些轨迹，TAPO在模型能力边界处引入难度感知的候选选择，并采用解耦优势估计以防止梯度污染。在AIME 2024、AIME 2025和HMMT 2025上的实验表明，在相同训练步数下，TAPO相比于GRPO取得了一致的改进。进一步分析表明，TAPO增强了首次推理和纠错有效性。

## Abstract
Self-distillation improves reasoning in large language models by using the model's own rollouts as training signal, typically through implicit logit-level alignment that minimizes KL divergence toward a privileged target distribution. However, because this supervision is generated via uncontrolled sampling, it provides no diagnostic insight into the model's specific errors or corrective guidance for its individual failure patterns. Consequently, the model learns to imitate a privileged distribution rather than receiving fine-grained corrections that pinpoint where and why its reasoning fails. In this paper, we propose Trajectory-Augmented Policy Optimization (TAPO), which advances self-distillation from implicit distributional alignment to explicit trajectory construction. During RL training, the model produces both correct and incorrect rollouts to the same query, and TAPO leverages this contrastive structure to construct micro-reflective corrections, new training trajectories that retain the model's erroneous reasoning up to the point of failure, then insert a natural-language diagnosis and corrected reasoning guided by a correct reference from the same sampling group. Since each trajectory is anchored in the learner's own prefix and solutions, the corrective signal preserves the model's on-policy distribution to a greater extent than the position-wise alignment imposed by KL-based methods. To integrate these trajectories, TAPO introduces difficulty-aware candidate selection at the model's capability boundary and decoupled advantage estimation to prevent gradient contamination. Experiments on AIME 2024, AIME 2025, and HMMT 2025 show that TAPO achieves consistent improvements over GRPO under the same number of training steps. Further analysis demonstrates that TAPO strengthens both first-pass reasoning and error-correction effectiveness.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **现有方法的局限**：当前大型语言模型通过自蒸馏（Self-Distillation）提升推理能力的主流方法（如KL散度对齐）存在两个根本缺陷：
  - **隐式且不可控的监督**：监督信号是连续token-level的logit分布，无法提供关于模型具体错误的位置、原因或如何纠正的显式诊断。
  - **错误被压制而非利用**：模型错误推理的前缀被视为需要被覆盖的内容，模型学会了避免错误状态，但从未训练如何从错误中恢复。
- **理论动机**：引入教育心理学中的“最近发展区（ZPD）”理论，认为有效学习必须锚定在学习者当前的理解（包括错误）上，并提供从错误到正确推理的显式路径。
- **整体目标**：提出Trajectory-Augmented Policy Optimization（TAPO），将自蒸馏从隐式分布对齐推进到显式轨迹构建，使模型从自身错误中学习可训练的微反思修正。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- 在RL训练中，对同一查询，模型同时生成正确和错误的展开（rollout）。TAPO利用这种对比结构，在失败点插入自然语言诊断和修正，构建**微反思轨迹**（micro-reflective trajectories）。
- 每条轨迹保留模型自身错误推理的前缀，然后由模型自身生成分析-修正内容，最后达到正确答案。
- 训练时构造轨迹，推理时仍采用标准单次生成，不增加推理成本。

### 2.2 关键技术细节
- **冷启动阶段（Cold-Start）**：在RL训练前，使用45,000个示例（30,000 SFT + 15,000 IFT）训练模型具备遵循轨迹构造提示和错误分析能力。
- **微反思轨迹构造（Micro-Reflective Trajectory Construction）**：在每个GRPO迭代中，对每个查询：
  - **难度感知候选选择（Difficulty-Aware Candidate Selection, DCS）**：当正确响应数 ≥ n_pos 且错误响应数 ≥ n_neg 时，认定该问题落在ZPD内。对每个合格查询，选择最多 m_max 个错误响应，每个配对随机采样一个正确参考。
  - **轨迹合成**：使用合成提示，要求模型分析错误位置和类型，然后重建轨迹：保留错误前缀直至第一个错误点，插入自然语言反思和修正，最终生成正确答案。
- **解耦优势估计（Decoupled Advantage Estimation, DAE）**：将原始GRPO组和构造轨迹组分别计算优势，避免构造轨迹中较高的正确率污染原始样本的优势，导致梯度污染和长度坍塌。
- **OOD Token抑制（OOD Token Suppression, OTS）**：对构造轨迹中的每个token，基于其log概率和策略熵计算分布对齐分数，转换为权重，抑制分布外token的梯度贡献，保持训练稳定。

### 2.3 算法流程（文字说明）
1. 对每个查询，从当前策略采样K个响应，获得奖励，划分为正确集P和错误集N。
2. 若满足ZPD条件，对每个合格错误响应与随机正确参考，通过模型自身生成微反思轨迹。
3. 分别计算原始组和反射组的优势（DAE）。
4. 对反射轨迹应用OTS权重。
5. 合并损失：L_TAPO = L_GRPO(原始组) + λ·L_ref(反射组, 带OTS)。

## 3. 实验设计

- **数据集与基准**：
  - 训练数据：DeepScaleR数据集（约40,000个数学问题-答案对），过滤后保留准确率在0.125~0.875之间的约16,000个查询。
  - 评估基准：AIME 2024（30题）、AIME 2025、HMMT 2025，均为高难数学竞赛。
- **基座模型**：Qwen3-8B-Instruct。
- **对比方法**：
  - 无RL基线：Qwen3-8B-Instruct（thinking模式开启/关闭）、Cold-Start检查点。
  - RL基线：GRPO、On-Policy Self-Distillation (OPSD)。
  - 两个训练设定：从基座模型直接训练、从冷启动检查点训练。
- **评估指标**：Pass@1~Pass@5（温度0.6，top-p 0.9，每个问题采样5次，16次独立评估取平均）。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息**。仅提到使用Qwen3-8B-Instruct作为基座，训练配置：GRPO组大小K=8，batch size=32，约500步，学习率1e-6，AdamW优化器。冷启动阶段训练3个epoch。未提及硬件细节。这属于论文中常见的信息缺失，但不影响方法理解。

## 5. 实验数量与充分性

- **实验数量充分**：
  - 主实验（表1）：在两个训练设定下，对比3个方法，在3个基准上报告Pass@1~Pass@5，共多个数据点。
  - 能力内化分析（表2）：DSR和ERR指标，在3个基准上对比GRPO和TAPO。
  - 消融实验（表3、表4）：包含轨迹结构（SFT vs RL、全重建 vs 微反思）、集成组件（DAE/OTS/负样本）的逐步消融。
  - 参数敏感性（表5）：ZPD阈值和构建数量的影响。
  - 训练动力学分析（图3）：6个子图展示OTS权重、损失、准确率、梯度范数、长度、KL散度。
- **实验设计公平客观**：
  - 所有RL方法在相同训练步数下比较。
  - 使用16次独立评估确保统计可靠性。
  - 对比方法包括基座模型（带/不带thinking模式）、冷启动、GRPO、OPSD，涵盖主流基线。
  - 消融实验逐步剥离组件，验证每个设计的必要性。
- **充分性评价**：实验覆盖了主要benchmark、多种对比、详细消融和动力分析，足以支撑核心结论。但缺少在更大规模模型（如7B以上）或更多样化任务（如编程、科学）上的验证，应用广度有限。

## 6. 论文的主要结论与发现

- TAPO在三个数学竞赛基准上，**在相同训练步数下一致优于GRPO和OPSD**，尤其在最难的HMMT 2025上提升显著（Pass@1提升2.71点 up to 31.46%）。
- **DSR（首遍推理正确率）提升显著**：TAPO的DSR相比GRPO在AIME 2024/2025/HMMT 2025上分别提升13.5/15.9/22.3个百分点，表明训练微反思轨迹增强了模型的首遍推理能力。
- **ERR（有效反思率）也提升**：在AIME 2024上升11.4点，说明模型学会了更有效的错误诊断和修正。
- **冷启动至关重要**：没有冷启动时，TAPO在困难基准上表现下降，OTS权重分析表明冷启动减少了OOD tokens。
- 每个集成组件（DCS、DAE、OTS、负样本）均贡献了正面效果，消融验证了其必要性。

## 7. 优点

- **方法论创新**：首次将自蒸馏从隐式logit对齐推进到显式、可读的轨迹构建，利用了GRPO组内天然的错误-正确对比信息，无需额外标注或教师模型。
- **原理清晰，有理论支撑**：基于ZPD理论设计ZPD候选选择，教育心理学视角赋予方法合理性。
- **训练-推理分离**：微反思轨迹仅用于训练，推理时单次生成，无额外开销，易于部署。
- **训练稳定性好**：OTD和DAE有效防止分布偏移和梯度污染，训练动力学曲线平滑，KL散度低。
- **实验严谨**：包含大量消融、参数敏感性、训练动力学分析，验证了各组件有效性。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在一个模型规模（8B）上验证，未在更大模型（如70B）或其他模型系列（如Llama）上测试。
  - 任务局限于数学竞赛，未在编程、科学推理等更广泛场景评估。
  - 训练数据仅来自DeepScaleR，未探索其他领域。
- **偏差风险**：
  - 冷启动阶段使用45k构造示例，其生成质量依赖于模型本身，可能存在自我强化偏差。
  - ZPD阈值（n_pos=2, n_neg=4）需要针对不同模型和任务调整，提出但不充分讨论通用性。
- **应用限制**：
  - 需要额外冷启动训练，增加训练复杂度。
  - OTS机制依赖熵和log概率计算，可能对策略更新敏感的模型引入额外超参数。
  - 未讨论在极低准确率或极高准确率场景下的表现（论文已排除此类数据）。
- **资源信息缺失**：未报告具体GPU型号、数量和训练时间，不利于复现。
- **性能提升幅度不大**：在AIME 2025上Pass@1与GRPO持平（46.88%），提升主要体现在HMMT (2.71点)，但Pass@5略逊。需要更多数据确认一致性。

（完）
