---
title: "On-Policy Distillation for Vision-Language Model Adaptation, an Effective Paradigm on Low-Quality Multimodal Data"
title_zh: 面向视觉-语言模型适配的在线策略蒸馏：一种在低质量多模态数据上的有效范式
authors: "Hongyuan Zhang, Xianda Guo, Yanlun Peng, Qianlong Yang, Yubin Guo, Pinhan Fu, Mulin Chen, Xiaozhen Qiao, Ping Luo"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10321v1"
tags: ["query:distillation"]
score: 10.0
evidence: 面向视觉语言模型适配的在线策略蒸馏
tldr: 视觉语言知识蒸馏常把教师预测固定为统一目标，在类别与域偏移下不可靠。OnPoKD 首次将on-policy蒸馏用于视觉语言模型适配，学习轻量控制器按样本动态构建蒸馏目标。控制器利用教师、学生与零样本先验的可靠性和分歧信号，通过有界策略动作平衡教师监督、先验引导和硬标签锚定。在Base-to-novel泛化与跨数据集迁移上，它稳定优于强蒸馏基线，且仅训练时使用，不改变推理架构与测试开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.10321v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10321v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1792, \"height\": 350, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10321v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1765, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10321v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1717, \"height\": 569, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10321v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 845, \"height\": 858, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.10321v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1791, \"height\": 1500, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10321v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 714, \"height\": 150, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10321v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 407, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10321v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1791, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10321v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1521, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10321v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1519, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有视觉语言蒸馏为所有样本固定教师预测目标，在类别和域偏移下不可靠，忽略样本动态可靠性。
method: OnPoKD学习轻量策略控制器，用教师、学生与零样本先验的可靠性和分歧线索，通过有界动作按样本动态生成蒸馏目标，并以验证反馈更新。
result: 在Base-to-novel泛化和跨数据集迁移基准上，OnPoKD持续超越强视觉语言蒸馏基线，且不改变推理架构与测试开销。
conclusion: 表明蒸馏目标构建可视为动态策略决策，为低质量多模态数据下的视觉语言模型适配提供有效范式。
---

## 摘要
知识蒸馏为将任务适配的视觉-语言教师模型迁移到紧凑学生模型提供了一条高效路径。当前视觉-语言蒸馏方法中的训练目标通常由教师预测构建，并统一应用于所有训练样本，这使其在类别和领域偏移下不可靠。在本文中，我们认为蒸馏目标的构建应被视为一种动态训练决策，而非固定方案。为此，我们提出 OnPoKD，一种用于视觉-语言模型适配的在线策略蒸馏框架。据我们所知，OnPoKD 是首个通过将目标构建学习为策略决策，从而将在线策略蒸馏应用于视觉-语言模型适配的框架。OnPoKD 学习一个轻量级控制器，该控制器利用来自教师模型、学生模型和零样本先验的可靠性线索与分歧线索，构建样本级自适应目标。控制器不依赖固定的教师预测，而是通过有界策略动作动态平衡教师监督、零样本先验引导和硬标签锚定，使蒸馏目标能够适应不同的样本可靠性和训练阶段。策略控制器通过验证反馈进行更新，促使目标构建优化可迁移性，而不仅仅是拟合训练分布。由于控制器仅在训练期间使用，OnPoKD 可以无缝集成到现有的视觉-语言蒸馏流程中，同时保持原始推理架构和测试时开销不变。在 Base-to-novel 泛化和跨数据集迁移基准上的大量实验表明，OnPoKD 持续优于强视觉-语言蒸馏基线。

## Abstract
Knowledge distillation offers an efficient route to transfer a task-adapted vision-language teacher to a compact student. The training target in current vision-language distillation methods is typically constructed from the teacher prediction and applied uniformly to all training samples, making it unreliable under class and domain shifts. In this paper, we argue that distillation target construction should be treated as a dynamic training decision rather than a fixed recipe. To this end, we propose OnPoKD, an on-policy distillation framework for vision-language model adaptation. To the best of our knowledge, OnPoKD is the first framework that applies on-policy distillation to vision-language model adaptation by learning target construction as a policy decision. OnPoKD learns a lightweight controller that constructs sample-wise adaptive targets using reliability and disagreement cues from the teacher model, student model, and zero-shot prior. Instead of relying on a fixed teacher prediction, the controller dynamically balances teacher supervision, zero-shot prior guidance, and hard-label anchoring through bounded policy actions, allowing the distillation target to adapt to varying sample reliability and training stages. The policy controller is updated with validation feedback, encouraging target construction to optimize transferability rather than merely fitting the training distribution. Since the controller is only used during training, OnPoKD can be seamlessly integrated into existing vision-language distillation pipelines while preserving the original inference architecture and test-time cost. Extensive experiments on Base-to-novel generalization and Cross-dataset transfer benchmarks show that OnPoKD consistently improves over strong vision-language distillation baselines.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：视觉-语言模型（如 CLIP）具备强零样本迁移能力，但下游任务常存在细粒度类别、域偏移和标签空间差异，需要适配。参数高效适配（Prompt、Adapter、Cache 等）可提升基类判别力，却可能损害开放词汇泛化。
- **核心问题**：现有视觉-语言知识蒸馏通常把教师预测作为固定训练目标，并统一应用于所有样本。该假设在类别偏移、域偏移和教师不可靠时失效：适配教师可能在基类准确，但在新类或偏移域上不可靠；冻结零样本先验虽在源域较弱，却保留开放词汇知识；学生训练过程中目标需求也会变化。
- **整体含义**：论文主张“蒸馏目标构建”不应是固定配方，而应视为动态训练决策。为此提出 **OnPoKD**，首次将 on-policy 蒸馏用于视觉-语言模型适配，通过轻量控制器按样本、按训练阶段自适应构建蒸馏目标。

## 2. 方法论

- **核心思想**：学习一个训练时策略控制器，根据教师、学生和零样本先验的可靠性/分歧线索，动态决定教师监督、零样本先验引导和硬标签锚定的混合比例，并可选调整样本权重与蒸馏温度。
- **监督来源**：
  - 适配教师 \(T\)：提供任务适配知识。
  - 冻结零样本先验 \(P\)：保留开放词汇泛化。
  - 硬标签 \(H\)：在软预测不可靠或冲突时提供锚定。
- **控制器状态**：从一次前向中提取统计量，包括教师/学生/先验的置信度、top-2 margin、归一化熵；教师-学生 KL 与 top-label 分歧；教师-先验 KL 与 top-label 冲突；教师-学生图像特征对齐。训练进度用于调制动作，使策略早期偏保守、后期逐步允许更强先验或标签干预。
- **策略动作**：
  - \(\lambda_i=(\lambda_T,\lambda_P,\lambda_H)\)：三路目标混合权重。
  - \(w_i\)：样本权重。
  - \(\tau_i\)：样本级蒸馏温度。
  - 动作通过有界投影、cap-and-renormalize 约束，保证先验和硬标签仅作为辅助监督，不能完全替代教师。
- **自适应目标**：
  - \(q_i=\lambda_T \cdot \text{softmax}(z_T/\tau_i)+\lambda_P q_P+\lambda_H q_H\)。
  - Base-to-novel 设置中，先验分量只作用于新类切片，基类仍由教师主导，以保护适配后的基类知识。
- **学生损失**：学生最小化加权 KL：\(w_i\tau_i^2 KL(q_i \| \text{softmax}(z_S/\tau_i))\)。
- **策略更新**：使用源域留出验证流，构建正确性、置信度、margin 等反馈信号 \(g_i\)，由确定性映射 \(F_{val}\) 生成目标动作 \(a_i^\star\)。策略损失匹配预测动作与目标动作，并加正则项。该更新与训练 KD 损失解耦，不通过学生优化器反传。
- **训练与推理**：控制器和先验分支仅在训练时使用；推理时移除，学生保持原适配流程的架构与测试开销。

## 3. 实验设计

- **基准设置**：
  - **Base-to-Novel Generalization**：在 11 个标准 CLIP 数据集上，仅用基类训练，评估基类、新类和调和平均 HM。
  - **Cross-Dataset Transfer**：在 ImageNet 上适配，迁移到 10 个目标数据集，评估域偏移下的 top-1 平均准确率。
- **数据集**：
  - Base-to-novel：ImageNet、Caltech101、OxfordPets、StanfordCars、Flowers102、Food101、FGVCAircraft、SUN397、DTD、EuroSAT、UCF101。
  - Cross-dataset：ImageNet 为源域，其余 10 个为目标域。
- **对比方法**：零样本 CLIP、CoOp、CoCoOp、MaPLe、PromptSRC、PromptKD。主要蒸馏基线为 PromptKD。
- **控制变量**：OnPoKD 保持教师、学生和测试时推理架构不变，仅改变训练时目标构建策略；超参基于源域或基类验证选择，固定后用于评估。
- **主要指标**：Base 准确率、Novel 准确率、HM、跨数据集平均准确率与各目标数据集准确率。

## 4. 资源与算力

- 文中未明确给出 GPU 型号、数量、总训练时长或总能耗。
- 仅报告了相对 PromptKD 的训练开销：
  - FGVCAircraft：训练时间 **+25%**，内存 **+51%**。
  - EuroSAT：训练时间 **+19%**，内存 **+51%**。
- 所有实验使用 **ViT-B/16 CLIP** 骨干。控制器为两层 MLP，带 LayerNorm，最终层零初始化。
- 因此，论文提供了相对训练开销，但缺少绝对算力与硬件配置说明。

## 5. 实验数量与充分性

- **实验规模**：
  - Base-to-novel：11 个数据集，含平均结果，共 12 组主要结果表。
  - Cross-dataset：ImageNet 到 10 个目标数据集。
  - 消融实验：
    - 监督来源贡献：8 个变体，在 FGVCAircraft、EuroSAT、DTD、EuroSAT Cross 上评估。
    - 策略动作贡献：5 个变体，在 FGVCAircraft、EuroSAT 上评估。
    - 可靠性线索贡献：5 个变体，在 FGVCAircraft、EuroSAT 上评估。
    - 超参敏感性：辅助目标 cap、策略正则、动作强度三组扫描。
    - 状态到动作映射可视化：FGVC-Aircraft 与 EuroSAT。
- **充分性评价**：实验覆盖类别偏移和域偏移两类核心场景，对比了多种强基线，并系统消融监督来源、动作空间和状态线索，整体较充分。
- **公平性**：控制教师、学生、推理架构一致，超参在源验证上选择，避免目标域调参；但主要结论基于单一 CLIP ViT-B/16 骨干，消融集中在少数代表性数据集，跨数据集仍有部分下降，说明并非所有场景都一致提升。

## 6. 主要结论与发现

- **Base-to-novel 泛化**：平均 HM 从 PromptKD 的 83.73 提升到 84.62；新类提升 **+1.40**，基类仅 **+0.30**，说明方法更有利于保护开放词汇迁移。
- **跨数据集迁移**：平均准确率达到 **72.66**，比 PromptKD 提升 **1.33%**；在 10 个目标数据集中的 7 个取得最佳，EuroSAT +5.69、DTD +3.11、Cars +2.11、Aircraft +1.79、Flowers102 +1.54。
- **消融结论**：
  - 固定教师-先验-标签混合不如自适应策略，说明“有哪些监督源”不如“何时信任哪个源”关键。
  - 去掉先验主要损害 Base-to-novel 的新类泛化；去掉硬标签主要损害跨数据集迁移。
  - 去掉验证反馈导致 FGVCAircraft HM 从 47.66 崩到 22.74，说明验证反馈对细粒度、教师易错场景至关重要。
  - 不确定性线索是最重要的状态信号；去掉后 FGVCAircraft HM 下降 9.69%，EuroSAT HM 下降 3.30%。
  - 自适应混合、样本权重、温度三者互补：混合决定“信什么”，权重决定“信多少”，温度决定“匹配多尖锐”。
- **超参发现**：有界干预有效；辅助 cap 过小或过大都损害 HM；策略正则和动作强度存在中间最优，跨数据集对干预规模更敏感。
- **总体结论**：有效视觉-语言蒸馏不仅需要多源监督，还需要机制决定各源何时、如何影响学生学习；OnPoKD 在训练时改进监督信号，推理时无额外成本。

## 7. 优点

- **问题切入新颖**：首次将 on-policy 蒸馏用于视觉-语言模型适配，把目标构建形式化为策略决策。
- **训练时策略、推理零开销**：控制器和先验分支仅在训练时使用，不改变学生推理架构与测试成本。
- **样本级与阶段级自适应**：同时考虑样本可靠性、教师-学生分歧、教师-先验冲突和训练进度，比固定教师目标更灵活。
- **多源监督平衡合理**：教师、零样本先验、硬标签各有明确角色，并通过有界动作防止不稳定干预。
- **验证反馈与训练损失解耦**：避免策略只优化短期训练损失，偏向可迁移目标构建。
- **实验较系统**：覆盖 Base-to-novel 与 Cross-dataset 两类偏移，消融监督源、动作空间、状态线索和超参，结论有较强支撑。
- **可视化与机制分析**：展示状态到动作映射，说明策略是数据集感知的，而非固定规则。

## 8. 不足与局限

- **骨干与任务范围有限**：仅使用 ViT-B/16 CLIP，主要验证分类任务；未验证更大骨干、检测/分割/生成等多模态任务。
- **训练开销增加**：相对 PromptKD，训练时间增加约 19%–25%，内存增加约 51%，对大规模训练不够轻量。
- **依赖源域验证流**：策略更新需要带标签的源域验证数据；若源验证不足或分布不匹配，反馈质量可能下降。
- **策略反馈图较手工**：\(F_{val}\)、\(\phi_P\)、\(\phi_H\) 为确定性非训练映射，超参较多，跨数据集表现非单调，调参成本存在。
- **跨数据集并非全胜**：Caltech101、Food101、UCF101 上相比 PromptKD 有轻微下降，说明当教师和先验都较自信时，干预可能过于激进。
- **“低质量多模态数据”验证不足**：论文标题强调低质量多模态数据，但实验主要是标准基准，未显式注入噪声、缺失模态或低质量图文对进行专项评估。
- **超参配置分离**：Base-to-novel 与 Cross-dataset 使用两套预设策略超参，虽在目标评估前固定，但统一性和泛化性仍可进一步检验。

（完）
